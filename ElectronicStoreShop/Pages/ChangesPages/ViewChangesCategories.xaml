<Page x:Class="ElectronicStoreShop.Pages.ChangesPages.ViewChangesCategories"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ElectronicStoreShop.Pages.ChangesPages"
      mc:Ignorable="d" 
      Style="{StaticResource MainPageStyle}"      
      Title="Изменения категорий">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Margin="10">
            <Label Content="Изменения категорий" 
                   Style="{StaticResource WindowTitleLabelStyle}"
                   HorizontalAlignment="Left"/>
            <Button Content="Назад" 
                    Style="{StaticResource GoBackButtonStyle}"
                    Click="Back_Click"
                    HorizontalAlignment="Right"/>
        </Grid>

        <StackPanel Grid.Column="0" Grid.Row="1" 
                    Style="{StaticResource StackPanelStyleProduct}"
                    Margin="10,0,5,10">
            <Button Content="Добавить категорию" 
                    Style="{StaticResource AddButtonStyle}" 
                    Click="AddCategory_Click"
                    Margin="0,0,0,10"/>
            <Button Content="Изменить категорию" 
                    Style="{StaticResource EditButtonStyle}" 
                    Click="EditCategory_Click"
                    Margin="0,0,0,10"/>
            <Button Content="Удалить категорию" 
                    Style="{StaticResource DeleteButtonStyle}"
                    Click="DeleteCategory_Click"/>
        </StackPanel>

        <ListView Grid.Column="1" Grid.Row="1" 
                  x:Name="CategoriesListView"
                  ItemsSource="{Binding Categories}"
                  SelectionMode="Single"
                  Margin="5,0,10,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource ChangesSectionBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" 
                                           FontWeight="Bold"
                                           FontSize="14"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
