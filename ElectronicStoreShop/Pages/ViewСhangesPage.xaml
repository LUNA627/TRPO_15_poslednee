<Page x:Class="ElectronicStoreShop.Pages.ViewСhangesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ElectronicStoreShop.Pages"
      mc:Ignorable="d" 
      Style="{StaticResource MainPageStyle}"
      Title="Изменения параметров товара">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Margin="10">
            <Label Content="Изменения параметров товара"  
           Style="{StaticResource WindowTitleLabelStyle}"/>

            <Button Content="Назад" 
            Style="{StaticResource GoBackButtonStyle}"
            Click="Back_Click"/>
        </Grid>

        <StackPanel Grid.Column="0" Grid.Row="1">
            <Button Content="Изменить товары" 
                Style="{StaticResource AddProductMenuChangeButtonStyle}" 
                Click="ChangesProduct_Click"/>

            <Button Content="Изменить категории" 
                Style="{StaticResource ManyProductMenuChangeButtonStyle}" 
                Click="ChangesCategory_Click"/>

            <Button Content="Изменить теги" 
                Style="{StaticResource ManyProductMenuChangeButtonStyle}" 
                Click="ChangesTag_Click"/>

            <Button Content="Изменить бренды" 
                Style="{StaticResource ManyProductMenuChangeButtonStyle}" 
                Click="ChangesBrand_Click"/>

            
        </StackPanel>

        <ListView Grid.Column="1" Grid.Row="1" 
          ItemsSource="{Binding Products}"
          Name="ProductsList">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource LookSectionBorderStyle}">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Название -->
                            <TextBlock Grid.Column="0" Grid.Row="0" 
                               Text="{Binding Name}" 
                               Style="{StaticResource ProductTitleTextStyle}"/>


                            <!-- Цена -->
                            <TextBlock Grid.Column="1" Grid.Row="0" 
                               Text="{Binding Price, StringFormat={}{0:F2} $}" 
                               Style="{StaticResource ProductPriceTextStyle}"/>

                            <!-- Рейтинг -->
                            <StackPanel Grid.Column="0" Grid.Row="1" 
                        Orientation="Horizontal">
                                <TextBlock Text="⭐" FontSize="14"/>
                                <TextBlock Text="{Binding Rating}" 
                        Style="{StaticResource ProductInfoTextStyle}"/>
                            </StackPanel>

                            <!-- Категория -->
                            <TextBlock Grid.Column="0" Grid.Row="2" 
                               Text="{Binding Category.Name}" 
                               Style="{StaticResource ProductInfoTextStyle}"/>

                            <!-- Бренд -->
                            <TextBlock Grid.Column="0" Grid.Row="3" 
                               Text="{Binding Brand.Name}" 
                               Style="{StaticResource ProductInfoTextStyle}"/>

                            <!-- Остаток -->
                            <TextBlock Grid.Column="1" Grid.Row="2" 
                               Text="{Binding Stock, StringFormat={}Остаток: {0} шт.}" 
                               Style="{StaticResource StrockInfoTextStyle}"/>


                            <!-- Теги -->
                            <TextBlock Grid.Column="0" Grid.Row="6" 
                        Text="{Binding DisplayTags}"
                        Style="{StaticResource TagInfoTextStyle}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
